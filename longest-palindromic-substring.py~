#!/usr/bin/python

class Solution(object):
    def longestPalindrome(self, s):
        size = len(s)
        if size < 2: return s
        cnt_tail = []
        even = [0] * size
        for i in range(size):
            
